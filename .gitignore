.DS_Store
node_modules/
**/node_modules/
dist/
.env
.env.local
.env.*.local

# spacetimedb / rust module build output
server/target/

# stray plugin artifacts
ruvector.db
agentdb.rvf
agentdb.rvf.lock
_probe_*.ts

# sidecar STDB identity token (privileged) — never commit
.sidecar-token

# test artifacts
chick-*.png
smoke-*.png
